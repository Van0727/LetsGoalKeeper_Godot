# 项目开发约定

## 中文功能注释

- 新增或修改 GDScript 代码时，必须同步添加或更新中文功能注释。
- 每个脚本应在文件开头说明模块职责；公开接口、关键状态、复杂规则和不直观分支应说明用途。
- 注释重点解释业务意图、结算顺序和边界条件，避免只把代码逐行翻译成中文。
- 行为发生变化时必须同步更新相关注释，禁止保留与实现不一致的过期说明。
- 测试脚本也应注明测试目标和关键场景，便于后续迁移时理解验收范围。

## Godot 问题处理与验收

- 遇到 Godot API、编辑器行为、资源加载或版本兼容问题时，必须先查阅对应版本的 Godot 官方文档，再制定或调整实现方案；不得只凭记忆连续试错。
- 每次实施前后必须主动检查潜在纰漏，包括但不限于 `@tool` 依赖链、编辑器缓存与重启要求、资源路径和覆盖范围、数据边界、类型转换、失败后的部分写入及存档兼容性。
- 实施完成后必须进行与风险相称的严谨自测，至少覆盖正常路径、关键边界和失败路径；无法在当前环境实际运行 Godot 测试时，必须明确标注未验证项，不得宣称测试通过，并提供可复现的验证步骤。

## 音频资源目录

- 所有背景音乐（BGM）必须放在 `sound/bgm`；该目录原名为 `beats`，新增或修改引用时不得继续使用旧路径。
- 所有效果音（SFX）必须放在 `sound/sounds`，包括切换背景音乐使用的 `changeBgm.mp3`。

## 配表与 CSV 资源规范

### 配表文件职责

- 项目策划配表统一存放在 `tables`。
- CSV 是数据源，`data` 下由导入工具生成的 `.tres` 和目录资源是运行时产物；修改数据时应优先修改 CSV，再运行统一导入工具，禁止只改生成资源导致两边不一致。
- 卡牌、战利品等正式数据必须通过现有导入器完成字段校验、资源生成和回读验证。
- 新增配表时，应同步提供中文说明行、字段名行和字段类型行，并在导入器中严格校验三行结构。

### CSV 的 Godot 导入方式

- `tables` 下用于策划数据的 CSV 不是本地化翻译表，对应 `.csv.import` 必须设置为：

  ```ini
  [remap]

  importer="keep"
  ```

- 禁止将策划配表设置为 `csv_translation`，否则 Godot 会把表头各列误认为语言并生成大量 `.translation` 文件。
- 修改或新增 CSV 后必须检查 Godot 实际导入器；不能仅凭文件扩展名推断导入方式。
- 配表目录中不应存在由策划 CSV 生成的 `.translation` 文件。若出现，应先把对应 CSV 改为 `importer="keep"`，再删除误生成文件并重新导入。
- 真正用于本地化的 CSV 应放入专门的本地化目录，不得与策划数据表混放。

### CSV 编码和格式

- 新增或修改含中文的策划 CSV 时统一保存为 UTF-8 BOM（前三字节 `EF BB BF`），方便 Windows 表格软件识别中文；字段顺序和类型必须与导入器声明完全一致。既有未修改文件的编码迁移应单独验证，不得顺手批量重写。
- 写入后必须按 UTF-8 严格解码回读，检查 BOM、中文说明行及代表性名称/文案的实际字符，不能只看终端输出或仅检查是否含 `�`：错误转码也可能留下合法 UTF-8 的乱码。发现乱码应从可信原文或版本记录恢复，禁止猜测性批量转码。
- 包含逗号、换行或双引号的文本必须使用合法 CSV 转义，禁止依赖手工字符串拆分。
- 不得随意修改字段名、字段类型或列顺序；契约变化必须同步更新导入器、测试和相关文档。
- 数字字段必须明确范围和用途，导入时校验整数、枚举、上下限及唯一性。
- 文件名字段只能使用安全的资源名，不得包含路径跳转、扩展名或不稳定字符。

### 数字 ID 规范

- CSV 第一列数字 `id` 是正式运行时唯一查找键；同类数据必须使用约定号段，并保证永久唯一。
- 已发布或进入存档的数据 ID 不得复用。资源被下架时应保留原 ID，使用禁用标记退出奖励池。
- 英文 ID 只允许作为资源文件关联、配表内部关联或旧存档迁移键；不得继续用于运行时查找、持有排重和特殊逻辑判断。
- 特殊玩法逻辑引用战利品时必须使用带语义名称的数字常量，禁止在业务代码中散落裸数字。
- 旧存档迁移映射必须保留；无法映射的旧值应安全忽略并记录诊断。

### 导入和修改流程

- 修改配表前先确认目标 ID、号段、字段类型、资源路径和受影响的存档契约。
- 修改完成后必须运行统一配表导入、生成资源回读校验、Godot 编辑器重新扫描或重新导入，以及对应领域的冒烟测试。
- 导入失败时不得继续使用部分生成结果；应检查是否发生部分写入，并重新执行完整导入。
- 导入后必须确认数据数量符合预期、ID 无重复或越界、引用目标存在、生成资源包含正确 ID 与正确中文文案、目录资源收录完整、禁用数据没有进入正式奖励池，并且 `tables` 下没有误生成的 `.translation` 文件。

### 测试要求

- 每类配表至少覆盖正常导入、缺失文件、错误表头、重复 ID、越界值、无效引用和生成资源回读。
- 涉及存档 ID 时，应覆盖新格式往返、旧格式迁移、重复旧值和无法映射值。
- 涉及奖励池时，应覆盖数字 ID 查找、持有排重、禁用项过滤和无效 ID。
- 测试中的临时资源也必须显式设置合法且不与正式数据冲突的测试 ID。

## 多代理职责边界

- 项目可按任务启用美术代理与代码代理；主代理负责拆分任务、确认交付路径并统一验收，`AGENTS.md` 只定义职责，不代表后台长期常驻一个代理。
- 美术代理只负责生成、裁切、缩放、检查和保存图片资源；除非用户另行批准，不得修改 GDScript、场景、卡牌数据、测试或其他逻辑文件。
- 代码代理只负责节点布局、资源字段、加载引用、交互逻辑和测试；不得擅自重新生成、重绘、抠图或覆盖已经确认的美术资源。
- 美术与代码任务需要并行时，必须使用互不重叠的文件范围；所有代理共享同一工作区，不得同时编辑同一文件。美术先固定文件名、尺寸和路径，代码再接入；变更规格后由主代理重新协调。
- 美术代理交付时必须报告最终图片尺寸、保存路径、生成提示词和是否经过裁切或缩放；代码代理验收时必须检查 Godot 导入状态、实际显示尺寸和资源缺失回退。

## 图片生成通用规范

- 生图前必须先向用户明确说明目标用途、最终像素尺寸、宽高比例、文件格式、保存路径和是否会进行裁切或缩放；未经用户确认不得调用图片生成工具，避免因尺寸判断错误浪费生成额度。
- 若同一需求包含多个素材，必须逐项列出每个素材的尺寸与用途；不得把插画槽尺寸、整张卡牌尺寸或 UI 徽章尺寸混用。

- 除卡牌插画等已明确采用 Alpha 透明的素材外，图片生成默认使用纯绿幕，不要求生成透明背景；绿幕颜色统一为 `#00FF00`，不得包含渐变、纹理、阴影、光晕或明显色差。卡牌插画以专门规范为准，不使用绿幕。
- 需要后续抠图的主体、描边、高光、反射和特效中不得使用与绿幕接近的绿色，边缘应清晰，避免绿色溢色影响抠图。
- 所有生成图片必须先确定游戏中的目标槽位比例和最终像素尺寸；生成结果的有效内容必须顶住目标边框，宽高方向不得保留无用边距、空白像素或仅用于构图的外围留白。
- 图片模型无法直接输出目标小尺寸时，应先按相同比例生成，再使用高质量缩放得到最终尺寸；放入项目的是最终尺寸文件，而不是模型原始大图。
- 用户完成抠图后，应保持相同文件名、像素尺寸和资源路径直接覆盖；覆盖后必须触发 Godot 重新导入并验证引用未失效。
- 生成图片不得包含未经要求的文字、数字、Logo、水印、卡框、按钮、费用标记或其他应由 Godot 动态绘制的 UI。

## 怪物图片生成与接入规范

### 文件与画布

- 生图前必须核对怪物在目标场景中的实际显示槽、逻辑尺寸和宽高比例，再确定源图及成品规格；不得把截图像素、编辑器缩放后的屏幕像素或其他类型素材的规格直接套用到怪物图片。
- 正式怪物图片统一使用安全的数字图片 ID 命名，保存到 `assets/ui/enemies`，文件名与怪物配表的 `image_id` 一致；候选图必须放在独立候选目录或使用明确版本后缀，用户确认前不得覆盖已接入资源。
- 正式成品使用 PNG RGBA；画布尺寸和有效内容范围以当前槽位及同类已确认资源为准。主体、必要特效和动态元素必须完整位于画布内，并保留足以容纳抗锯齿的安全边距，不得贴边、裁切或保留与构图无关的大块空白。
- 图片模型无法直接输出目标尺寸时，必须先按相同比例生成，再使用高质量重采样得到最终文件；不得拉伸画面，也不得把模型原始大图直接放进项目依赖运行时缩小。

### 视觉设计

- 怪物应与现有游戏界面的表现精细度、形体概括方式和光影复杂度协调；规范约束视觉关系，不固定轮廓颜色、具体色相、物种、身体结构或装饰类型。
- 每个怪物应具有容易识别的整体轮廓、稳定的视觉重心和少量明确的标志性特征。缩小到实际游戏尺寸后，角色表情、主要形体与关键状态仍应清楚，避免依赖细线、微小纹理或高密度装饰表达差异。
- 配色应建立清楚的主体、次要区域和强调区域关系，保证相邻形体及角色与战斗背景之间有足够区分；不得机械复用同一套颜色，也不得为了“高级感”将角色无依据地去色、压暗或改成单色。
- 高光、暗面、投影和材质细节应服务于形体识别，保持层级简洁；避免写实纹理、颗粒噪点、过度渐变、复杂反射和与游戏界面不协调的电影化光效。
- 动感可通过姿态、形变、重心、轮廓趋势、动态线或局部效果表达。具体方式必须服从角色设定和行为特点，不强制出现音符、乐器、运动器材或任何固定身体部位。
- 参考图只用于约束用户明确要求的画风、观察角度、精细度或构图关系，不得未经要求复制参考图的具体配色、主题物件、角色结构和背景元素。

### 已确认扁平画风标准

- 当前第一章普通怪物以 `assets/ui/enemies/7001.png` 和已确认的乌龟扁平化方案为画风基准：使用扁平移动端插画、大色块、清晰深蓝描边、少量干净的块面阴影和图形化高光；参考图只约束扁平程度、形体概括和渲染语言，不得照搬角色造型与配色。
- 需要正视角的怪物必须严格面对镜头：面部、胸腹和主要形体围绕垂直中轴组织，双眼同高且大小接近，身体两侧可见量与主要肢体落点近似对称；不得用偏头、侧脸、身体转向或三分之四透视代替正视角。允许耳朵、尾巴、局部特效等次要元素在不破坏主体正视关系的前提下形成轻微差异。
- 外轮廓使用清晰、稳定的深蓝粗描边；内部线条只保留识别结构和表情所需部分，避免用大量细线、缝隙、纹理或装饰性碎线制造复杂度。缩小到实际战斗槽位后，轮廓、表情和主要色块仍须清楚。
- 每种主要材质原则上使用约二至三个明确色阶，以大面积纯色和边界清楚的块面塑造形体。禁止依赖连续柔和渐变、复杂环境反射、写实材质或密集色阶表现体积。
- 光影统一为左上受光、右下暗面；高光应缩小为少量干净的图形色块，暗面使用克制的块面阴影。不得出现大面积镜面高光、玻璃感反射、塑料质感、电影化轮廓光或为了增强立体感而堆叠的渐变。
- 配色必须保留明确的主体色、辅助色和强调色关系，明亮但克制；扁平化不等于去色、灰化或压暗。斑点、材质纹理与装饰细节只保留对角色识别有帮助的部分，不得抢占表情和整体轮廓的视觉优先级。
- 对已确认角色进行风格迁移时，只允许调整上色、光影、描边和细节密度；必须锁定用户已确认的观察角度、姿态、表情、比例、主体轮廓、配色关系和画布占比。若确需改变其中任一项，应先另行说明并获得用户确认。
- 当前第一章普通怪物正式成品统一为 `512×512`、PNG RGBA，并为挤压、弹跳、旋转和震动保留安全边距。输出后必须检查四角透明、边缘无绿幕残色，并在实际约 `205×153` 的战斗头像显示区分别以浅色和深色背景验证小尺寸辨识度。

### 背景、抠图与边缘

- 怪物源图默认遵循图片生成通用规范使用纯 `#00FF00` 绿幕；绿幕必须覆盖全部背景和四角，保持完全纯色，不得包含渐变、纹理、投影、光晕或色差。用户明确批准直接生成透明图时，成品必须具有真实 Alpha，不能以黑底、白底或棋盘格伪装透明。
- 绿幕版本中的主体、轮廓、反射、特效和半透明区域不得使用接近绿幕的颜色；若角色设计确实需要绿色，应先与用户确认改用另一种可可靠分离的背景或直接透明流程。
- 抠图必须保留主体及用户确认的动态元素，清除绿幕溢色、孤立像素、锯齿杂点和无意义的半透明残留。不得通过粗暴扩大或缩小 Alpha 掩盖边缘问题，也不得误删与背景颜色相近的有效内容。
- 正式 PNG 的完全透明区域不得残留会在线性过滤、缩放或生成 Mipmap 后形成绿边、黑边的错误颜色；轮廓边缘允许合理的抗锯齿过渡。接入前须在透明底、浅色底和深色底分别检查边缘。

### 生成提示词结构

- 怪物提示词按“用途与槽位、主体概念、性格与行为、整体轮廓、观察角度与构图、视觉表现方式、配色关系、动感或特效、背景、禁止内容”组织；各项使用当前需求的可替换描述，不得把单个已确认怪物的特征写成所有怪物的固定模板。
- 提示词必须明确实际用途和小尺寸可读性，要求主体占据合理画面范围、有效内容完整且没有无用留白，并明确禁止未经要求的文字、数字、Logo、水印、UI、边框、平台、复杂场景和主题无关元素。
- 同批方案应通过轮廓、重心、表情、姿态、配色关系或标志性特征形成实质差异，禁止只换颜色或添加少量装饰就作为不同方案交付。

### 动画适配

- 计划使用整图程序动画时，主体轮廓、表情区域和必要动态元素应清楚且避免过度遮挡，画布须为挤压、弹跳、旋转和震动保留安全范围；不得为了预设动画擅自改变用户已经确认的构图。
- 计划使用分层、骨骼或逐帧动画时，必须在生图前另行确定分层边界、遮挡关系、关节覆盖、帧数和导出规格；静态怪物规范不能替代动画素材规范。
- 眨眼、局部形变等依赖特定区域坐标的程序效果必须按怪物单独配置并提供安全关闭方式，不得假定所有怪物拥有相同结构或表情位置。

### 接入与验收

- 接入时优先修改图片源文件及正式数据来源：图片 ID 写入怪物 CSV 后运行统一导入器，回读生成资源并确认 `image_id` 正确；禁止仅修改生成的 `.tres` 或在场景上叠加另一张图片规避正式引用。
- 覆盖同路径图片后必须触发 Godot 重新导入，并同时检查编辑器预览、运行时资源加载、实际显示尺寸、过滤方式和缺失资源回退。源图分辨率正确不能代替真实场景验收。
- 成品回读至少检查 PNG 格式、像素尺寸、RGBA 模式、Alpha 有效边界、四角透明、主体安全边距、异常半透明像素和边缘颜色；再以实际游戏尺寸检查辨识度、裁切、遮挡、锯齿及背景对比。
- 用户只批准候选图时不得修改游戏引用；用户确认接入后应保持图片 ID、文件名和资源路径稳定。若必须改变规格或路径，应先说明受影响的配表、编辑器预览、缓存和运行时加载范围。
- 交付时必须报告最终尺寸、格式、保存路径、生成提示词、是否裁切或缩放、Godot 重新导入结果、实际场景验证结果及未验证项。

## 卡牌插画生成规范

### 尺寸与文件

- 当前 `CardView/Canvas/Illustration` 显示槽位为 `85.416664×79` 个逻辑像素，卡内位置为 `(9, 16)`，比例约 `1.081:1`；游戏基准画面为 `360×640`。窗口缩放后的屏幕像素与逻辑像素必须区分。
- 最终素材统一为 `342×316` 像素、`171:158` 比例、PNG RGBA 格式，约为当前显示槽位的四倍分辨率；源图分辨率不决定运行时显示尺寸，不得继续套用旧的 `200×150`、4:3 或 `176×132` 交付规格。
- 生图前必须重新核对实际槽位；比例变化时重新确定素材规格。先按目标比例生成，再高质量重采样至最终尺寸，不拉伸整幅画面、不裁掉主体、不留无用边距。
- 插画统一保存到 `assets/ui/cards`。候选图使用版本后缀，用户确认前保留已接入的资源；确认替换后同步更新引用。

### 画风与配色

- 以已确认参考图与 `card_gloves_v5.png` 为画风基准：扁平移动端 UI 插画、大色块、清晰深蓝描边、少量干净的块面阴影和高光。参考图只参考画风与质感，不照搬配色、构图或足球素材。
- 保留明确色相，适度降低饱和度，明亮但不刺眼；“高级灰”表示克制的饱和度与配色，不代表灰色、去色、单色或压暗画面。
- 避免写实材质、布料纹理、颗粒、复杂缝线、复杂明暗、过度渐变、镜头光晕和电影化光效。

### 主体与光影

- 根据 `tables/cards.csv` 的名称和效果独立设计主体、轨迹与构图，不能仅换背景颜色或套用同一足球模板。主体足够大，以一个主要视觉主题为核心，避免堆积元素；不强制每张都居中。
- 同图多个足球必须有不同球面朝向、旋转角度和透视大小，禁止复制同一个球再缩放；同批不同卡牌也不得机械重复球面布局。
- 统一左上受光、右下投影：物体左上添加少量亮面，右下添加侧面暗部，并向背景投下清晰、克制的阴影。
- 高光、暗面和投影采用简洁块面与少量色阶，增加质感同时保持扁平感；投影不得扩大画布、挤压主体或形成外围留白。
- 缩小到实际游戏尺寸后，主体、轮廓和光影层次仍须清晰可辨；避免细线与高密度细节。

### 效果区分与运动形变

- 当前已确认的四张攻击牌设计方向：香蕉球为紫色背景与明显 C 形回弯；挑射为青蓝背景、高抛弧线与简化球门；连续射门为蓝色背景、多个球与独立速度拖尾；爆炸球为珊瑚橙背景、单球与集中爆裂块面。后续卡牌按自身效果独立设计，不把这四种构图套到全部卡牌。
- 运动形变按效果决定：挑射保持自然圆球，不压扁；香蕉球、连续射门和爆炸球可沿飞行方向轻微拉伸、倾斜，各球形变程度可不同，禁止全部圆球或全部统一压扁。
- 形变必须克制且仍清楚是足球，不得变成橄榄球、蛋形或扁盘。球面、高光与暗部随形变调整；拖尾接到球后侧，避免圆球悬停感。
- 用户要求局部修改时，只改明确指定部分，锁定其余构图、配色和主体；未经批准不得借修补重新设计。

### 圆角与透明区域

- 生图阶段只生成完全不透明的直角矩形，背景铺满四边和全部四角，不使用绿幕；不得让生图模型生成透明、半透明、圆角面板或孤立主体贴纸。
- 原图验收合格并高质量缩小后，再用固定程序复制 `card_gloves_v5.png` 的 Alpha 蒙版统一切圆角；`342×316` 成品圆角半径约 `32` 像素，以手套实际轮廓为准，不让模型决定圆角规格。
- 最终四圆角面板外接边界与画布四边相切，不添加外围透明边距或阴影占位；用户明确暂不处理圆角时，交付完全不透明矩形 PNG。
- 面板内部 Alpha 必须为 `255`，仅四个圆角外侧透明，轮廓边缘允许抗锯齿过渡；不使用绿幕，也不因旧绿幕规则禁止正常的绿色配色。
- 禁止额外透明边距、内部镂空、黑色空洞、背景缺失或主体区域缺失。主体、拖尾、特效与投影必须位于面板内。
- 卡框、费用、名称、类型、说明、数值、按钮等由 Godot 动态绘制；插画不得包含文字、数字、徽章、Logo、水印或其他 UI。

### 生成与验收流程

- 生图前明确目标卡牌、用途、最终尺寸、比例、格式、路径，以及裁切或缩放方案，并按图片生成通用规范取得确认。
- 同批素材先生成一张样张确认风格，再扩展到其他卡牌；交付时报告最终提示词、路径、尺寸及实际裁切或缩放情况。
- 原始生图先逐像素检查完全不透明性，再目视检查背景缺色、黑洞与杂讯；坏图不得作为成品交付，禁止强制改 Alpha 掩盖颜色缺失，也不得反复交付未经验收的草稿。
- 成品回读检查尺寸、颜色模式、内部 Alpha 为 `255`、透明仅在四角外侧、圆角与手套蒙版一致、有效内容无额外边距；查看实际槽位尺寸预览，核对球面差异、效果辨识度与形变幅度。
- 保存候选图不等于批准接入：用户确认前不覆盖已接入资源、不修改游戏引用；失败草稿不得混入正式交付文件。
- 接入时同步检查卡牌资源引用、场景纹理和 `@tool` 的 `editor_preview_definition`；工具脚本的 `configure()` 会覆盖场景纹理，不能仅修改 TextureRect 的静态纹理就宣称编辑器预览已更新。
- 接入后必须完成 Godot 重新导入及真实场景渲染，检查显示范围、圆角、主体清晰度与光影效果。平滑插画使用线性过滤，避免最近邻过滤造成像素台阶；实际设置应以当前 Godot 版本文档与运行验收为准。
- 配表导出必须保留已确认的插画引用；CSV 未声明插画字段时，美术引用单独维护，不为换图擅自改变数值表契约，导出后回读验证引用未被清空。
- 无法实际渲染时必须明确标注未验证项，不得仅凭资源路径、源图尺寸或节点配置宣称游戏内效果已通过。

## 参考图尺寸换算

- 从整屏参考图复刻 UI 时，应先读取参考图的原始像素尺寸，再测量目标元素的实际像素边界；不得凭视觉估算素材尺寸。
- 通用换算公式为：`目标宽度 = 参考元素宽度 × 目标屏幕宽度 ÷ 参考图宽度`，高度同理；横纵缩放比例存在微小截图误差时，应结合目标槽位比例统一取整。
- 原战斗 UI 参考图为 `941×1672`；应按当前目标视口重新计算缩放系数，不得继续套用历史 `720×1280` 视口的约 `0.765` 系数或 `176×132` 示例。当前已经调整的卡牌插画槽以项目节点实际配置为准，参考图继续用于画风与构图对照。
- 截图换算得到的是运行时显示尺寸，不能证明源素材是否包含透明边距、九宫格边缘或高倍源图。交付分辨率须另行明确；卡牌插画按专门规范使用约四倍源图，不将显示槽位尺寸直接当作素材源尺寸。

## 回答格式约定

- 每次回答的最后必须附上本次实际运行的模型名，格式为：`模型：<实际模型ID>`。
- 模型名不得写死，也不得把当前模型误报成其他厂商或其他版本；无法确认时应明确写“未知”，不能猜测。

## GitHub 提交规范

- 每次功能完成并通过相应验收后，助手必须判断当前改动是否已构成可独立回滚、可验证的阶段性成果，并在回复中明确写出“建议提交”或“暂不建议提交”及简要原因。
- 只有用户明确授权后，才能执行 `git add`、`git commit` 或推送到远端；未经授权不得创建本地提交或修改远端分支。
- 提交信息必须包含简洁标题和正文说明，不能只写标题。正文应列出本次实现的主要功能、关键数据或资源变更、实际运行的测试，以及未验证项和原因。
- 提交前必须检查待提交文件范围、工作区状态和目标分支；发现与当前任务无关的改动时，必须先向用户说明并确认是否一并提交。

## 实施前方案与模型选择

- 每次开始修改代码前，必须先向用户给出简明实施方案。
- 提交方案后必须等待用户明确批准；在获得批准前，不得修改任何项目文件、运行会产生写入或状态变更的工具，也不得开始实际实现。
- 用户提出修改需求本身不等同于批准实施方案；只有用户在看到方案后明确回复批准、确认或同等含义的指令，才可以开始执行。
- 如果用户请求的第一个有效字符是全角感叹号 `！`，视为用户已直接批准该请求对应的实施方案。助手仍须先简明说明实施方案、影响范围、模型建议及是否建议新开对话，但无需再次询问或等待批准，可以在同一轮继续修改、运行工具并完成验收。
- `！` 直接批准规则仅识别忽略请求开头空白后的全角感叹号；半角感叹号 `!` 不触发直接批准。未以 `！` 开头的请求继续遵循先提交方案、再等待用户明确批准的规则。
- 如果批准后发现需要明显扩大范围、改变架构方案或增加原方案未说明的高风险操作，必须暂停并重新提交方案等待批准。
- 方案中必须明确说明本次改动是否涉及架构、核心状态机、跨场景数据契约或存档结构。
- 方案中必须说明是否建议使用高级模型，并根据任务性质给出模型建议：
  - 架构设计、跨模块重构、复杂调试和高风险迁移优先使用 `gpt-5.6-sol`；
  - 日常功能开发、常规重构和需要兼顾质量与成本的任务优先使用 `gpt-5.6-terra`；
  - 卡牌数值、敌人配置、文案、重复性资源创建等内容填充任务优先使用 `gpt-5.6-luna`。
- 模型建议只用于帮助用户选择，不得宣称当前会话已经切换模型；实际模型仍以运行环境提供的信息为准。
- 助手没有擅自切换当前会话模型或推理强度的权限；除非用户明确要求并由运行环境实际完成切换，否则不得用推荐模型冒充实际模型。
- 回答末尾的模型信息必须报告当前会话实际运行模型，而不是本次方案中的推荐模型；无法从运行环境确认时必须写“未知”，用户明确告知当前模型时应以该信息为准。
- 每次给出实施方案时，必须同时说明是否建议新开对话，并简要说明判断理由。
- 新对话建议应综合考虑是否进入新的开发阶段、当前上下文是否过长、任务是否与当前主题明显不同，以及新对话能否提高执行效率和准确率。
- 新开对话仅作为建议，不替代用户对实施方案的明确批准；在获得批准前仍不得修改项目文件或执行会产生写入、状态变更的操作。
