# 项目开发约定

## 中文功能注释

- 新增或修改 GDScript 代码时，必须同步添加或更新中文功能注释。
- 每个脚本应在文件开头说明模块职责；公开接口、关键状态、复杂规则和不直观分支应说明用途。
- 注释重点解释业务意图、结算顺序和边界条件，避免只把代码逐行翻译成中文。
- 行为发生变化时必须同步更新相关注释，禁止保留与实现不一致的过期说明。
- 测试脚本也应注明测试目标和关键场景，便于后续迁移时理解验收范围。

## Godot 问题处理与验收

- 遇到 Godot API、编辑器行为、资源加载或版本兼容问题时，必须先查阅对应版本的 Godot 官方文档，再制定或调整实现方案；不得只凭记忆连续试错。
- 每次实施前后必须主动检查潜在纰漏，包括但不限于 `@tool` 依赖链、编辑器缓存与重启要求、资源路径和覆盖范围、数据边界、类型转换、失败后的部分写入及存档兼容性。
- 实施完成后必须进行与风险相称的严谨自测，至少覆盖正常路径、关键边界和失败路径；无法在当前环境实际运行 Godot 测试时，必须明确标注未验证项，不得宣称测试通过，并提供可复现的验证步骤。
- 修改 Godot 代码、场景、资源或导入流程后，必须按 [`doc/agent_guides/godot_verification.md`](doc/agent_guides/godot_verification.md) 选择并执行与风险相称的验收。

## 音频资源目录

- 所有背景音乐（BGM）必须放在 `sound/bgm`；该目录原名为 `beats`，新增或修改引用时不得继续使用旧路径。
- 所有效果音（SFX）必须放在 `sound/sounds`，包括切换背景音乐使用的 `changeBgm.mp3`。

## 专项规则文档路由

- 修改策划 CSV、导入器或生成资源前，必须完整阅读 [`doc/agent_guides/csv_and_import_rules.md`](doc/agent_guides/csv_and_import_rules.md)。
- 修改存档格式、本局状态、正式数字 ID 或旧档迁移前，必须完整阅读 [`doc/agent_guides/save_compatibility.md`](doc/agent_guides/save_compatibility.md)。
- 生成、处理或接入任何游戏图片，或从整屏参考图复刻 UI 前，必须完整阅读 [`doc/agent_guides/image_asset_rules.md`](doc/agent_guides/image_asset_rules.md)。
- 生成、处理或接入怪物图片前，必须完整阅读 [`doc/agent_guides/monster_art_rules.md`](doc/agent_guides/monster_art_rules.md)。
- 生成、处理或接入卡牌插画前，必须完整阅读 [`doc/agent_guides/card_art_rules.md`](doc/agent_guides/card_art_rules.md)。
- 主代理向子代理分派任务时，必须使用 [`doc/agent_guides/agent_task_contract.md`](doc/agent_guides/agent_task_contract.md) 中的任务契约。
- 专项规则索引及适用范围见 [`doc/agent_guides/README.md`](doc/agent_guides/README.md)。根文件只保留全局规则与路由，专项文档是对应领域细则的唯一事实来源。

## Skill 使用审批规则

- 所有任务默认不使用 Skill；不得仅因存在匹配 Skill 就自动读取、调用或展开对应流程。
- 每次回复结尾必须在模型信息之前写出 `必须使用的 Skill：<Skill 名称或“无”>`。这里只列出完成当前请求确实不可缺少的 Skill，不得把可选、推荐或仅用于增强质量的 Skill 列为必须。
- 判断必须使用某个 Skill 时，应先向用户说明 Skill 名称、必要原因及预计增加的流程；在用户明确回复“批准使用 Skill”或同等含义前，不得读取该 Skill 的 `SKILL.md`、创建对应子代理或执行 Skill 流程。
- 用户在请求中直接点名某个 Skill，视为已经批准本次使用该 Skill，无需重复申请。
- 用户只批准实施方案，不等同于批准使用 Skill；实施审批与 Skill 审批必须分别判断。
- 未获 Skill 批准时，继续使用常规代码检查、项目文档和必要测试完成任务；不得因未使用 Skill 而擅自扩大流程或阻塞可以直接完成的小任务。
- 多个 Skill 都被判断为必须时，应一次性列明并统一申请批准；不得先后追加审批来扩大流程。
- 用户明确禁止某个 Skill 时，本任务不得使用；若因此确实无法安全完成，应说明阻塞原因并停止，不得静默改用同类 Skill。

## 多代理职责与文件所有权

- 主代理负责跨系统设计、任务拆分、文件所有权分配、最终验收和 Git 决策；专职代理只负责其稳定领域，临时子代理只承担边界明确的独立子任务。
- 只有子任务目标明确、文件范围可限定、不过度依赖变化中的中间状态，且委派确实能降低上下文负担或并行调查时，才创建子代理；小任务默认由主代理直接完成。
- 同一时间只能有一个代理负责写入某个文件；多个代理可以只读检查同一文件。并行任务的允许修改范围必须互不重叠。
- 子代理需要扩大文件范围时必须暂停并报告，不得自行扩大；不得执行 Git 提交、推送、重置、清理或切换分支。
- 美术代理只负责获批范围内图片的生成、裁切、缩放、抠图、检查和保存；除非用户另行批准，不得修改 GDScript、场景、CSV、生成资源或测试。
- 代码代理负责获批范围内的节点布局、资源字段、加载引用、交互逻辑和测试；不得擅自生成、重绘、抠图或覆盖已确认的美术资源。
- 美术与代码任务需要协作时，美术先固定文件名、尺寸和路径，代码再接入；规格变化后由主代理重新协调。
- 美术代理交付时必须报告最终尺寸、格式、保存路径、生成提示词及裁切或缩放情况；代码验收必须检查 Godot 导入状态、实际显示尺寸和资源缺失回退。

## 回答格式约定

- 每次回答的最后必须附上本次实际运行的模型名，格式为：`模型：<实际模型ID>`。
- 模型名不得写死，也不得把当前模型误报成其他厂商或其他版本；无法确认时应明确写“未知”，不能猜测。

## GitHub 提交规范

- 每次功能完成并通过相应验收后，助手必须判断当前改动是否已构成可独立回滚、可验证的阶段性成果，并在回复中明确写出“建议提交”或“暂不建议提交”及简要原因。
- 只有用户明确授权后，才能执行 `git add`、`git commit` 或推送到远端；未经授权不得创建本地提交或修改远端分支。
- 提交信息必须包含简洁标题和正文说明，不能只写标题。正文应列出本次实现的主要功能、关键数据或资源变更、实际运行的测试，以及未验证项和原因。
- 提交前必须检查待提交文件范围、工作区状态和目标分支；发现与当前任务无关的改动时，必须先向用户说明并确认是否一并提交。

## 实施前方案与模型选择

- 每次开始修改代码前，必须先向用户给出简明实施方案。
- 提交方案后必须等待用户明确批准；在获得批准前，不得修改任何项目文件、运行会产生写入或状态变更的工具，也不得开始实际实现。
- 用户提出修改需求本身不等同于批准实施方案；只有用户在看到方案后明确回复批准、确认或同等含义的指令，才可以开始执行。
- 如果用户请求的第一个有效字符是全角感叹号 `！`，视为用户已直接批准该请求对应的实施方案。助手仍须先简明说明实施方案、影响范围、模型建议及是否建议新开对话，但无需再次询问或等待批准，可以在同一轮继续修改、运行工具并完成验收。
- `！` 直接批准规则仅识别忽略请求开头空白后的全角感叹号；半角感叹号 `!` 不触发直接批准。未以 `！` 开头的请求继续遵循先提交方案、再等待用户明确批准的规则。
- 如果批准后发现需要明显扩大范围、改变架构方案或增加原方案未说明的高风险操作，必须暂停并重新提交方案等待批准。
- 方案中必须明确说明本次改动是否涉及架构、核心状态机、跨场景数据契约或存档结构。
- 方案中必须说明是否建议使用高级模型，并根据任务性质给出模型建议：
  - 架构设计、跨模块重构、复杂调试和高风险迁移优先使用 `gpt-5.6-sol`；
  - 日常功能开发、常规重构和需要兼顾质量与成本的任务优先使用 `gpt-5.6-terra`；
  - 卡牌数值、敌人配置、文案、重复性资源创建等内容填充任务优先使用 `gpt-5.6-luna`。
- 模型建议只用于帮助用户选择，不得宣称当前会话已经切换模型；实际模型仍以运行环境提供的信息为准。
- 助手没有擅自切换当前会话模型或推理强度的权限；除非用户明确要求并由运行环境实际完成切换，否则不得用推荐模型冒充实际模型。
- 回答末尾的模型信息必须报告当前会话实际运行模型，而不是本次方案中的推荐模型；无法从运行环境确认时必须写“未知”，用户明确告知当前模型时应以该信息为准。
- 每次给出实施方案时，必须同时说明是否建议新开对话，并简要说明判断理由。
- 新对话建议应综合考虑是否进入新的开发阶段、当前上下文是否过长、任务是否与当前主题明显不同，以及新对话能否提高执行效率和准确率。
- 新开对话仅作为建议，不替代用户对实施方案的明确批准；在获得批准前仍不得修改项目文件或执行会产生写入、状态变更的操作。
